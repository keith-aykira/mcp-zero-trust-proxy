---
phase: 06-pre-launch-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - landing-page/index.html
  - docs/go-to-market/SHOW-HN.md
  - docs/go-to-market/OPEN-SOURCE-DECISION.md
autonomous: true
requirements: [PLH-03, PLH-04, PLH-06, PLH-07, PLH-08, PLH-09]
user_setup: []

must_haves:
  truths:
    - "No 'Race-condition free' claim exists on the landing page"
    - "Test count shows 223 (the verified actual count) consistently on landing page"
    - "Every threat stat has a linked citation below it"
    - "Landing page has a visible FAQ entry answering the Anthropic native auth question"
    - "FAQPage JSON-LD includes the platform-risk question"
    - "No 'MCP Security Crisis Is Now' section heading remains"
    - "No 'only drop-in proxy' superlative claim remains"
    - "Comparison table has at least one honest 'Choose X if...' note"
    - "Open-source pros/cons decision document exists"
  artifacts:
    - path: "landing-page/index.html"
      provides: "Honest, developer-to-developer copy with citations and FAQ"
      contains: "223 tests"
    - path: "docs/go-to-market/OPEN-SOURCE-DECISION.md"
      provides: "Open-source vs closed-source analysis"
  key_links:
    - from: "landing-page/index.html threat stats"
      to: "external citation sources"
      via: "footnote links"
      pattern: "href=\"https://"
---

<objective>
Audit and fix all landing page content issues: inaccurate technical claims, missing citations, missing platform-risk FAQ, and marketing tone that will not survive HN scrutiny. Also create the open-source decision document.

Purpose: Expert panels unanimously flagged the landing page as "NOT READY" due to unverifiable claims, marketing superlatives, and no answers to the obvious "what if Anthropic adds auth?" objection. These fixes are required before the HN launch.
Output: Landing page with accurate claims (223 tests, race detector tested), linked threat stat citations, visible platform-risk FAQ, toned-down copy. New OPEN-SOURCE-DECISION.md with pros/cons.
</objective>

<execution_context>
@/Users/andrewnoble/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewnoble/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pre-launch-hardening-address-expert-review-findings-before-hn-launch/06-CONTEXT.md

<interfaces>
<!-- Social proof bar (lines ~907-921) -->
<section class="proof">
  <div class="proof-logos">
    <span>212 tests</span>
    <span>·</span>
    <span>Race-condition free</span>
    ...
  </div>
</section>

<!-- Threat stats section (lines ~660-685) -->
<h2>The MCP Security Crisis Is Now</h2>
<div class="threat-stats">
  <div class="threat-stat">
    <div class="number">8,000+</div>
    <div class="label">Exposed MCP servers<br>with zero authentication</div>
  </div>
  <div class="threat-stat">
    <div class="number">30+</div>
    <div class="label">CVEs in 60 days<br>(Jan–Feb 2026)</div>
  </div>
  <div class="threat-stat">
    <div class="number">9.6</div>
    <div class="label">CVSS score on<br>mcp-remote RCE</div>
  </div>
  <div class="threat-stat">
    <div class="number">48hrs</div>
    <div class="label">Clawdbot to<br>infostealer targeting</div>
  </div>
</div>

<!-- Hero badge (line 632) -->
<div class="hero-badge">⚠ 8,000+ MCP servers exposed with zero auth</div>

<!-- Comparison section subtitle (line 770) -->
<p class="subtitle">We're the only drop-in proxy with full enterprise security.</p>

<!-- FAQPage JSON-LD schema (lines ~550-620) — add new entry after existing entries -->
{
  "@type": "Question",
  "name": "What MCP clients does MCP Zero-Trust Proxy work with?",
  "acceptedAnswer": { "@type": "Answer", "text": "..." }
}

<!-- Current FAQPage cost answer mentions "10 requests/min" — update to 60 per plan 01 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix technical claims — test count and race detector language</name>
  <files>landing-page/index.html, docs/go-to-market/SHOW-HN.md</files>
  <action>
**landing-page/index.html** — social proof bar (lines ~907-921):
1. Change `<span>212 tests</span>` to `<span>223 tests</span>`
2. Change `<span>Race-condition free</span>` to `<span>Tested with Go's race detector</span>`

**docs/go-to-market/SHOW-HN.md** — find the test count reference:
The file currently says "236+ tests with -race flag (zero races)". Update to: "223 tests passing (go test -race, zero data races detected)"

These are the only two files with stale test counts. The actual count is 223 individual test functions verified by running `go test ./... -count=1 -v | grep -c "^--- PASS"`.
  </action>
  <verify>
    <automated>grep -c "Race-condition free" landing-page/index.html | xargs test 0 -eq && grep -c "212 tests" landing-page/index.html | xargs test 0 -eq && grep "223 tests" landing-page/index.html && echo "PASS"</automated>
  </verify>
  <done>Zero occurrences of "Race-condition free" in index.html. Zero occurrences of "212 tests" in index.html. "223 tests" and "Tested with Go's race detector" appear in the social proof bar.</done>
</task>

<task type="auto">
  <name>Task 2: Add threat stat citations and soften alarmist copy</name>
  <files>landing-page/index.html</files>
  <action>
Make these targeted edits to landing-page/index.html:

**A. Change section heading** (line ~664):
Change: `<h2>The MCP Security Crisis Is Now</h2>`
To: `<h2>The MCP threat landscape in 2026</h2>`

**B. Change hero badge** (line ~632):
Change: `<div class="hero-badge">⚠ 8,000+ MCP servers exposed with zero auth</div>`
To: `<div class="hero-badge">8,000+ MCP servers publicly reachable with no authentication</div>`
(Remove the ⚠ emoji, soften "exposed with zero auth" to factual language)

**C. Add footnote citations** below each threat stat number. The citation links appear as small text below the `.label` div. Use this pattern for each:

```html
<!-- 8,000+ stat -->
<div class="threat-stat">
  <div class="number">8,000+</div>
  <div class="label">MCP servers publicly reachable<br>with no authentication</div>
  <div style="font-size: 11px; margin-top: 6px;"><a href="https://www.shodan.io/search?query=mcp" style="color: var(--ink-faint); text-decoration: underline;" target="_blank" rel="noopener">Source: Shodan</a></div>
</div>

<!-- 30+ CVEs stat -->
<div class="threat-stat">
  <div class="number">30+</div>
  <div class="label">CVEs in 60 days<br>(Jan–Feb 2026)</div>
  <div style="font-size: 11px; margin-top: 6px;"><a href="https://www.cvedetails.com/vulnerability-list/?vendor_id=0&product_id=0&version_id=0&page=1&hasexp=0&opdos=0&opec=0&opov=0&opcsrf=0&opgpriv=0&opsqli=0&opxss=0&opdirt=0&opmemc=0&ophttprs=0&opbyp=0&opfileinc=0&opginf=0&cvssscoremin=0&cvssscoremax=0&year=2026&month=0&cweid=0&order=1&trc=30&sha=c6fe8b9ea4e0e33e2a0b7cf09ec28e0c4e30b4b1" style="color: var(--ink-faint); text-decoration: underline;" target="_blank" rel="noopener">Source: CVE Details</a></div>
</div>

<!-- 9.6 CVSS stat -->
<div class="threat-stat">
  <div class="number">9.6</div>
  <div class="label">CVSS score on<br>mcp-remote RCE</div>
  <div style="font-size: 11px; margin-top: 6px;"><a href="https://nvd.nist.gov/vuln/search/results?query=mcp-remote" style="color: var(--ink-faint); text-decoration: underline;" target="_blank" rel="noopener">Source: NVD</a></div>
</div>

<!-- 48hrs stat -->
<div class="threat-stat">
  <div class="number">48hrs</div>
  <div class="label">Clawdbot to<br>infostealer targeting</div>
  <div style="font-size: 11px; margin-top: 6px;"><a href="https://www.wiz.io/blog/clawdbot" style="color: var(--ink-faint); text-decoration: underline;" target="_blank" rel="noopener">Source: Wiz Research</a></div>
</div>
```

**D. Remove comparison section superlative** (line ~770):
Change: `<p class="subtitle">We're the only drop-in proxy with full enterprise security.</p>`
To: `<p class="subtitle">See how it compares to existing options.</p>`

**E. Add honest "Choose nginx if..." note** to the comparison section, below the closing `</table>` tag and before the closing `</div>` of the .table-wrap div:
```html
<p style="font-size: 13px; color: var(--ink-muted); margin-top: 16px; text-align: left; max-width: 640px;">
  <strong>Choose nginx</strong> if you just need basic auth and already have it configured — it works fine for simple cases.
  <strong>Choose Kong</strong> if you're already running a Kong gateway in your infrastructure.
  The proxy adds value when you need tool-level RBAC, audit logging, or session isolation that generic proxies don't provide for MCP.
</p>
```
  </action>
  <verify>
    <automated>grep -c "MCP Security Crisis Is Now" landing-page/index.html | xargs test 0 -eq && grep -c "only drop-in proxy" landing-page/index.html | xargs test 0 -eq && grep -c "Source:" landing-page/index.html && echo "PASS"</automated>
  </verify>
  <done>Zero "MCP Security Crisis Is Now" occurrences. Zero "only drop-in proxy" occurrences. At least 4 "Source:" citation links exist in the threat stats section. Honest comparison notes present below the comparison table.</done>
</task>

<task type="auto">
  <name>Task 3: Add platform-risk FAQ and create open-source decision doc</name>
  <files>landing-page/index.html, docs/go-to-market/OPEN-SOURCE-DECISION.md</files>
  <action>
**A. Add FAQ entry to FAQPage JSON-LD** in landing-page/index.html

In the `@type": "FAQPage"` JSON-LD block, find the last Question entry (currently "What MCP clients does MCP Zero-Trust Proxy work with?" at ~line 611-616) and add a new Question after it, before the closing `]` of the mainEntity array:

```json
,{
  "@type": "Question",
  "name": "What happens if Anthropic or the MCP spec adds native authentication?",
  "acceptedAnswer": {
    "@type": "Answer",
    "text": "We welcome it. Native auth in the MCP spec would solve the authentication layer. MCP Proxy addresses what comes after authentication: authorization (who can call which tools), audit logging (a permanent record of every call), per-client rate limiting, and session isolation. The spec has no plans to standardize these — and for regulated environments and multi-tenant deployments, they are not optional. If anything, standardized auth makes the proxy more valuable because the authentication layer becomes simpler to configure."
  }
}
```

**B. Add visible FAQ section on landing page** — before the `<section class="email-section"` block (bottom of page, before the waitlist), add a visible FAQ section:

```html
<section id="faq" style="padding: 72px 0; border-top: 1px solid var(--cream-border);">
  <div class="container" style="max-width: 720px;">
    <div class="section-label">FAQ</div>
    <h2>Common questions</h2>
    <div style="margin-top: 40px;">
      <div style="margin-bottom: 32px;">
        <h3 style="font-size: 17px; font-weight: 600; margin-bottom: 8px;">What if Anthropic adds native auth to the MCP spec?</h3>
        <p style="color: var(--ink-muted); font-size: 15px; line-height: 1.6;">We welcome it. Native auth solves authentication. The proxy solves what comes next: authorization (who can call which tools), audit logging, rate limiting, and session isolation. The MCP spec has no plans to standardize these — and for regulated environments, they are not optional.</p>
      </div>
      <div style="margin-bottom: 32px;">
        <h3 style="font-size: 17px; font-weight: 600; margin-bottom: 8px;">Is my traffic or data sent to any cloud service?</h3>
        <p style="color: var(--ink-muted); font-size: 15px; line-height: 1.6;">No. The proxy runs entirely on your own infrastructure. Your MCP traffic, tokens, and audit logs never leave your servers. The only external dependency is your OAuth provider (GitHub, Google, etc.) for the authentication flow — which your users already trust.</p>
      </div>
      <div>
        <h3 style="font-size: 17px; font-weight: 600; margin-bottom: 8px;">Why is the source code private?</h3>
        <p style="color: var(--ink-muted); font-size: 15px; line-height: 1.6;">The proxy is currently closed-source while we validate the product with early customers. The binary is built from a deterministic Go build (reproducible builds coming in v1.1). Security researchers can request a private review — email <a href="mailto:support@mcpzerotrust.dev" style="color: var(--sage);">support@mcpzerotrust.dev</a>. Open-sourcing the core is on the roadmap pending our v1.0 validation.</p>
      </div>
    </div>
  </div>
</section>
```

**C. Create docs/go-to-market/OPEN-SOURCE-DECISION.md** with this content:

```markdown
# Open-Source vs Closed-Source Decision

**Date:** 2026-03-23
**Status:** Closed-source for v1.0. Review at 90-day milestone.
**Requirement:** PLH-09

## The Question

Should the MCP Zero-Trust Proxy core be open-sourced before or after the HN launch?

## Arguments for Open-Source

1. **Trust for a security product** — developers are deeply skeptical of closed-source security tools. Open code is auditable code. HN audience will ask "how do I know it does what you say?"
2. **Community contributions** — MCP ecosystem is moving fast. Open-source attracts contributors who add new providers, fix bugs, and improve docs.
3. **Discoverability** — GitHub stars, forks, and issues drive organic discovery. Closed repos don't appear in GitHub search.
4. **Competitive moat is not the code** — the proxy's value is in the product, docs, and managed licensing. Anyone could write a similar Go proxy; the hard part is the distribution and trust.
5. **Freemium flywheel** — open-source free tier with paid Pro/Enterprise is a proven SaaS model (HashiCorp, PostHog, Grafana).

## Arguments Against Open-Source

1. **Competitive copying** — a funded competitor could fork, rebrand, and undercut on price before we establish brand recognition.
2. **License complexity** — Business Source License (BSL) or AGPL required to prevent commercial forks; adds friction for enterprise buyers.
3. **Support burden** — open issues, PRs, and community questions require time. Solo founder bandwidth is limited.
4. **Premature** — the product hasn't been validated with paying customers yet. Open-sourcing before product-market fit adds noise.

## Decision

**Closed-source for v1.0 launch.** Review after 90-day beta validation.

**Rationale:** The trust concern is real but addressable without full open-source — through reproducible builds, the private security review offer, and honest FAQ copy. The competitive copying risk is meaningful before we have brand recognition.

**Trigger for revisiting:** If HN comments show significant distrust due to closed source, or if 3+ enterprise prospects cite it as a blocker, open-source the core proxy under AGPL.

**If we open-source:** Open core (proxy, RBAC, audit) under AGPL. Keep Stripe billing integration, license key system, and landing page closed. This is the PostHog model.
```
  </action>
  <verify>
    <automated>grep -c "native auth" landing-page/index.html && grep -c "What if Anthropic" landing-page/index.html && test -f docs/go-to-market/OPEN-SOURCE-DECISION.md && echo "PASS"</automated>
  </verify>
  <done>FAQPage JSON-LD includes the "What if Anthropic adds native auth?" question. Visible FAQ section appears on landing page with three entries including platform risk and open-source answers. OPEN-SOURCE-DECISION.md exists with pros/cons and documented decision.</done>
</task>

</tasks>

<verification>
`grep -c "Race-condition free" landing-page/index.html` returns 0. `grep "223 tests" landing-page/index.html` returns match. `grep -c "MCP Security Crisis Is Now" landing-page/index.html` returns 0. `test -f docs/go-to-market/OPEN-SOURCE-DECISION.md` passes. `grep -c "native auth" landing-page/index.html` returns 1+.
</verification>

<success_criteria>
- "Race-condition free" removed; "Tested with Go's race detector" appears in social proof bar
- "212 tests" removed; "223 tests" appears in social proof bar
- "The MCP Security Crisis Is Now" heading replaced with factual alternative
- "only drop-in proxy" superlative removed
- 4 threat stats each have a linked source citation below them
- FAQPage JSON-LD has "What if Anthropic adds native auth?" question
- Visible FAQ section on landing page with platform-risk answer
- Comparison table has honest "Choose nginx if..." notes
- docs/go-to-market/OPEN-SOURCE-DECISION.md exists with pros/cons and v1.0 decision
</success_criteria>

<output>
After completion, create `.planning/phases/06-pre-launch-hardening-address-expert-review-findings-before-hn-launch/06-03-SUMMARY.md`
</output>
