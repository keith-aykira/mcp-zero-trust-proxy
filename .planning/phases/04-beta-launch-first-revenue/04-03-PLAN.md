---
phase: 04-beta-launch-first-revenue
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - landing-page/index.html
  - docs/go-to-market/SHOW-HN.md
  - .github/workflows/release.yaml
  - docs/QUICKSTART.md
autonomous: false
requirements: [BETA-01, BETA-03, BETA-04]

must_haves:
  truths:
    - "Landing page at mcpzerotrust.dev shows pricing tiers with working Stripe checkout links"
    - "Docker image is pullable from GHCR and includes license key support from Plan 01"
    - "Show HN post is drafted with breach data angle and ready to publish"
    - "QUICKSTART.md explains free tier usage and how to add a license key for paid tiers"
  artifacts:
    - path: "landing-page/index.html"
      provides: "Updated landing page with pricing section and Stripe checkout links"
      contains: "pricing"
    - path: "docs/go-to-market/SHOW-HN.md"
      provides: "Ready-to-post Show HN submission"
      contains: "Show HN"
    - path: ".github/workflows/release.yaml"
      provides: "GitHub Actions workflow to build and push Docker image to GHCR"
      contains: "ghcr.io"
    - path: "docs/QUICKSTART.md"
      provides: "Updated quick-start with license key instructions"
      contains: "license"
  key_links:
    - from: "landing-page/index.html"
      to: "Stripe checkout"
      via: "Payment links from Plan 02's setup-stripe-products.sh output"
      pattern: "checkout\\.stripe\\.com"
    - from: ".github/workflows/release.yaml"
      to: "Dockerfile"
      via: "Builds Docker image with license-key-aware binary"
      pattern: "docker build"
---

<objective>
Deploy the landing page with pricing to mcpzerotrust.dev, publish the Docker image to GHCR for self-serve pulls, update QUICKSTART.md with license key instructions, and draft the Show HN post for launch day.

Purpose: This is the launch plan. Everything goes live: landing page with pricing + checkout links, Docker image publicly pullable, Show HN drafted and ready to post. After this plan, the product is live and generating revenue.

Output: Live landing page, pullable Docker image, updated docs, Show HN draft.
</objective>

<execution_context>
@/Users/andrewnoble/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewnoble/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-beta-launch-first-revenue/04-CONTEXT.md
@.planning/phases/04-beta-launch-first-revenue/04-01-SUMMARY.md
@.planning/phases/04-beta-launch-first-revenue/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: license config in YAML -->
```yaml
license:
  key: "${LICENSE_KEY}"  # or empty for free tier
```

<!-- From Plan 02: Stripe payment link URLs (substituted at deploy time) -->
<!-- Pro checkout: https://buy.stripe.com/XXXXXX (from setup-stripe-products.sh output) -->
<!-- Enterprise checkout: https://buy.stripe.com/YYYYYY (from setup-stripe-products.sh output) -->

<!-- From existing Dockerfile -->
```dockerfile
FROM golang:1.25-alpine AS builder
# ... builds mcpproxy binary
FROM alpine:3.19
ENTRYPOINT ["mcpproxy"]
CMD ["--config", "/etc/mcpproxy/config.yaml"]
```

<!-- Existing outreach templates -->
@docs/go-to-market/OUTREACH-MESSAGES.md (Version B = Show HN template)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page pricing section + GHCR release workflow + QUICKSTART update</name>
  <files>landing-page/index.html, .github/workflows/release.yaml, docs/QUICKSTART.md</files>
  <action>
    1. Update `landing-page/index.html` — add a pricing section BEFORE the waitlist form:
       - Three-column pricing grid (responsive: stack on mobile)
       - **Free** tier: $0/month — 1 MCP server, 10 req/min, stdout audit, community support. CTA: "Get Started" linking to QUICKSTART.md on GitHub.
       - **Pro** tier (highlighted/recommended): $49/month — 5 MCP servers, 200 req/min, file audit + rotation, email support. CTA: "Start Pro" linking to Stripe checkout (use placeholder `STRIPE_PRO_LINK` that user replaces with actual payment link from Plan 02 output).
       - **Enterprise** tier: $199/month — Unlimited servers, unlimited rate, all features, priority support, SSO (coming soon). CTA: "Start Enterprise" linking to Stripe checkout (placeholder `STRIPE_ENTERPRISE_LINK`).
       - Match existing landing page design system (navy/dark-blue/accent colors from CSS vars)
       - Add "Founding member pricing — locked in for life" badge/note above the pricing grid
       - Keep the existing waitlist form below pricing (for people who want updates but aren't ready to buy)

    2. Create `.github/workflows/release.yaml`:
       - Trigger: push tag matching `v*` (e.g., v1.0.0)
       - Jobs:
         a. **test**: Run `go test -race ./...` — gate for release
         b. **release-docker** (needs: test): Build Docker image using existing Dockerfile, push to `ghcr.io/anoblescm/mcp-zero-trust-proxy:$TAG` and `:latest`. Use `docker/login-action` with `GITHUB_TOKEN` for GHCR auth.
         c. **release-binary** (needs: test): Build static binary for linux/amd64, linux/arm64, darwin/amd64, darwin/arm64. Create GitHub Release with binaries attached. Use `CGO_ENABLED=0` and `-ldflags="-s -w -X main.version=$TAG"`.
       - Set `permissions: packages: write, contents: write` for GHCR push and release creation.

    3. Update `docs/QUICKSTART.md` — add a "License Key" section after the config example:
       - Explain three tiers (free = no key needed, pro, enterprise)
       - Show how to add license key to config.yaml:
         ```yaml
         license:
           key: "${LICENSE_KEY}"
         ```
       - Show docker run with license key: `docker run -e LICENSE_KEY=your-key-here ...`
       - Mention: "Get a license key at mcpzerotrust.dev" with link
       - Keep the rest of QUICKSTART.md unchanged
  </action>
  <verify>
    <automated>cd /Users/andrewnoble/Developer/mcp-zero-trust-proxy && grep -q "pricing" landing-page/index.html && echo "pricing section OK" && grep -q "ghcr.io" .github/workflows/release.yaml && echo "release workflow OK" && grep -q "license" docs/QUICKSTART.md && echo "quickstart updated OK"</automated>
  </verify>
  <done>Landing page has pricing section with 3 tiers and Stripe checkout placeholders, GHCR release workflow builds and pushes on tag, QUICKSTART.md documents license key usage</done>
</task>

<task type="auto">
  <name>Task 2: Draft Show HN post and community launch materials</name>
  <files>docs/go-to-market/SHOW-HN.md</files>
  <action>
    Create `docs/go-to-market/SHOW-HN.md` — the ready-to-post Show HN submission:

    **Title:** "Show HN: MCP Zero-Trust Proxy — drop-in auth for AI tool servers in 5 minutes"
    (Must be under 80 chars for HN)

    **URL:** https://mcpzerotrust.dev

    **Body** (adapt from OUTREACH-MESSAGES.md Version B, with these updates):
    - Lead with the breach data: "8,000+ exposed MCP servers, 30+ CVEs in 60 days, the Clawdbot breach hit 1,800+ servers in 48 hours"
    - Explain the problem: MCP auth is optional in the spec, 75% of servers built by individuals with no security review
    - What it does: Drop-in reverse proxy, zero code changes. OAuth 2.1 PKCE, tool-level RBAC, session isolation, audit logging, rate limiting.
    - One-liner demo: `docker run -e MCP_TARGET=localhost:3000 ghcr.io/anoblescm/mcp-zero-trust-proxy`
    - Tech details HN cares about: Go, single binary (10MB), 177 tests with race detection, YAML config, ECDSA license keys with zero network calls
    - Pricing: Free for 1 server. Pro $49/mo (5 servers). Enterprise $199/mo (unlimited).
    - Open source core (note: repo is currently private — decide whether to open-source at launch or keep private; include both versions of the paragraph)
    - Ask: "Looking for feedback from anyone running MCP servers in production"
    - Include link to GitHub repo and docs

    Also add a "Community Seeding Checklist" section at the bottom:
    - [ ] Post to HN (weekday, 8-10am ET)
    - [ ] Post to r/aiagents (use Version A from OUTREACH-MESSAGES.md)
    - [ ] Post to r/SaaS (use validation post from OUTREACH-MESSAGES.md)
    - [ ] Post to MCP Discord (use Version A from OUTREACH-MESSAGES.md)
    - [ ] Post to Claude Code Community Discord
    - [ ] DM 10 developers who posted about MCP security (use Template A from OUTREACH-MESSAGES.md)
    - [ ] Tweet thread summarizing the breach data + product
  </action>
  <verify>
    <automated>cd /Users/andrewnoble/Developer/mcp-zero-trust-proxy && test -f docs/go-to-market/SHOW-HN.md && grep -q "Show HN" docs/go-to-market/SHOW-HN.md && echo "Show HN draft OK"</automated>
  </verify>
  <done>Show HN post drafted with breach data angle, one-liner demo, pricing, and technical details. Community seeding checklist included with all channels and templates referenced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review complete launch package before go-live</name>
  <action>Pause for user review of the full launch package. User verifies landing page pricing looks correct, release workflow is valid, QUICKSTART has license instructions, and Show HN reads well. User then executes the go-live steps (Vercel deploy, Stripe link replacement, git tag for release, HN post).</action>
  <verify>User confirms approval and executes go-live steps</verify>
  <done>User has approved the launch package. Landing page deployed to mcpzerotrust.dev, Docker image published to GHCR, Show HN ready to post.</done>
  <how-to-verify>
    1. Open landing-page/index.html in browser — verify pricing section looks correct
    2. Review .github/workflows/release.yaml — verify it builds for correct platforms
    3. Review docs/QUICKSTART.md — verify license key instructions are clear
    4. Review docs/go-to-market/SHOW-HN.md — verify the post reads well
    5. Execute go-live: replace Stripe placeholders, `vercel --prod`, `git tag v1.0.0 && git push origin v1.0.0`, post Show HN
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 4 complete, or describe changes needed</resume-signal>
</task>

</tasks>

<verification>
- Landing page loads correctly with pricing section visible
- Release workflow YAML is valid GitHub Actions syntax
- QUICKSTART.md documents free tier and license key usage
- Show HN post follows HN formatting conventions and uses real data from research phase
- All Stripe checkout link placeholders are clearly marked for replacement
</verification>

<success_criteria>
- Landing page has responsive 3-tier pricing section matching existing design
- GitHub Actions release workflow produces Docker image on GHCR and binary releases
- QUICKSTART.md explains license key config for all tiers
- Show HN draft ready to post with breach data angle
- Community seeding checklist covers all channels from OUTREACH-MESSAGES.md
- User has reviewed and approved the full launch package
</success_criteria>

<output>
After completion, create `.planning/phases/04-beta-launch-first-revenue/04-03-SUMMARY.md`
</output>
